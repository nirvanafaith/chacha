# 安全修复交付状态

## 已实现

- 格式 2：完整明文清单认证，块 AAD 绑定文件总长/块数/编号；明确拒绝未认证的格式 1。
- Windows 路径严格校验，拒绝越界、ADS、保留名、重解析点及冲突路径；有界解析与资源限额。
- 加密/还原独占暂存、无覆盖发布；失败仅清理本任务输出，保护已有包和目标。
- 密钥独占暂存、同步、回读验证、无覆盖保存。GUI 保存失败保留内存密钥，提供另存、关闭警告，不误报成功。
- 同步错误传播、源文件前后状态检查、扫描错误不再静默忽略。
- 文件及目录/根目录修改时间认证和恢复；目录句柄与设置错误处理修正。
- 精确 u64 字节进度、避免任务取整和 32 位截断；SIMD 自测明确分派。
- 两端底部算法/CPU/线程提示完全移除，文件列表高度增加 24 个逻辑像素；维持合理三列宽度。
- 修正密钥另存默认扩展名，避免重复追加复合扩展名。

## 验证

- Rust 1.44.0 i686 GNU 发布构建通过，两端 EXE 已更新到 dist。
- Rust 单元测试 2/2 通过（独立 SIMD 路径、进度边界）。测试构建禁用与 libtest 符号冲突的 XP shim；发布仍启用。
- 安全回归 12 组、421 次 CLI 调用，零失败/错误。
- CLI 往返、错误密钥、密文篡改：ALL PASS。
- GUI 主流程：31/31 PASS，包括无密钥预览和取消语义。
- 密钥写入故障：已有密钥保护、未保存状态、拒绝关闭、另存恢复、明确确认关闭通过。
- 两端 --self-test 通过。
- 两端布局 LAYOUT OK；列宽加密端 356/95/145，解密端 376/95/145（本机 DPI）。
- PE 静态审计通过：x86 PE32、GUI 5.01。此结论不等于 XP 实机验证。
- 截图位于 target/enc-final.png 和 target/dec-final.png；当前模型不支持图片输入，未做人工视觉判断。

测试入口：tools/security-regression.py、tools/verify.py、tools/drive-gui.ps1、tools/key-save-gui.ps1、tools/inspect.ps1、tools/pe.py。可用 CHACHA_TEST_DIST 指定 CLI/主 GUI 回归的待测分发目录。

## 升级注意

新版只接受格式 2；旧程序已保存在 legacy-v1，带风险说明，仅用于隔离恢复本人可信的旧格式包。旧包不会自动迁移。不要对不可信旧包使用旧版，旧版仍存在审查报告所述风险。

## 尚未完成或不作保证

- 包与外部密钥跨路径/跨卷的崩溃原子提交；CLI 密钥落盘失败会返回失败，但包可能已经发布，原件应保留。GUI 恢复需要进程仍存活。
- 断电后的自动恢复、同权限恶意进程主动路径竞态、私有 ACL 加固、VSS 原子快照。
- ACL、ADS、硬链接关系、创建时间及 1970 年前时间的无损备份；普通删除不保证安全擦除。
- 独立密码学审计、模糊测试、完整 XP/CPU/文件系统及高 DPI 矩阵。
- 后台扫描/虚拟列表、用户列宽持久化、密钥内存/分页清零等后续增强。
- 当前使用系统 msvcrt.dll，不是完全静态 CRT。资源限额和保守名称限制见 README。

SECURITY_REVIEW_AND_IMPROVEMENT_PLAN.md 保留为修复前审查快照；不得将本轮交付描述为其中全部长期改进均已完成。
