# 更新日志

## v2.1.0

首次在本仓库发布，基于格式 2 的安全加解密实现，新增限次密钥与铁道资源解密品牌文字。

### 新功能

- 加密端增加 1–7 次可用次数选择，默认 1 次；CLI 增加 `--uses` 参数。
- 新 `CHAKY002` 密钥封装隐藏计数字段。每次开始整包解密前独占读取、扣次、同步写入并读回验证；只读或被占用时拒绝操作。
- 最后一次还原正常完成，之后读取提示「密钥次数已耗尽」并禁用解密；重启保持耗尽状态。
- 完整封装的密钥复制和保存失败后的另存恢复保留原始次数，不受后续界面选择影响。
- 解密端窗口标题和抬头改为「铁道资源解密」，选择加密包提示去掉「（文件夹 B）」。

### 下载

- `chacha-v2.1.0-windows-x86.zip`：加密端和解密端完整便携包。
- `chacha-enc-v2.1.0-windows-x86.zip`：仅加密端。
- `railway-resource-dec-v2.1.0-windows-x86.zip`：仅解密端。
- `SHA256SUMS.txt`：下载完整性校验。

解压全部文件后运行 EXE；请保留同目录的 `.exe.manifest` 文件。无需安装。程序未作代码签名。

### 兼容性与重要说明

- Windows x86 32 位，使用系统 `msvcrt.dll`。PE 静态检查满足 XP 5.01 目标，但没有完成 XP 实机与全部 CPU/DPI 矩阵验证。
- 新包为格式 2，拒绝旧格式 1 包；旧版程序不随此 Release 分发。
- 旧 `CHAKY001` 和裸密钥仍可读取，但不限次；旧程序不能读取新版限次密钥。
- 导入/预览不扣次；实际扣次后取消、数据认证失败或异常退出不退次数。密钥写回中断可能损坏密钥。
- 限次是离线文件管理，不是不可绕过的授权。备份回滚、不同副本、重新复制加密端密钥或修改程序均可绕过；各副本不共享次数。
- 文件名、目录结构、大小和时间可公开预览；请分开保管密钥与加密包，保留独立备份。

### 验证

- Rust 单元测试：8 项。
- 密钥次数回归：10 组，覆盖 1–7、默认值、耗尽重启、并发、只读、完整封装文本导入及空包。
- 安全回归：12 组、421 次 CLI 调用。
- 原生 GUI：次数选择、复制、保存失败恢复、最后一次还原、耗尽重开及窗口文字检查。
- Windows PE 静态兼容性检查。

这些检查不能替代独立密码学审计或目标系统实机验收。
