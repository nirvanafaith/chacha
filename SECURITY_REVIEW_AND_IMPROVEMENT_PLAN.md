# 文件夹加解密系统安全审查与改进方案

## 1. 审查范围与结论

本次审查针对当前工作区中的双程序实现：`chacha-enc`、`chacha-dec`，包含包格式、加解密文件操作、Win32 UI、并发、构建及测试脚本。保留 XChaCha20-Poly1305、无需密钥预览文件名、Rust 1.44、32 位和 XP 兼容的产品方向。

**结论：列宽问题已经修正，但当前版本仍存在应优先修复的数据丢失和清单信任边界问题，不宜仅凭往返测试通过就作为重要文件的唯一备份。**

本报告以源码证据和调用链分析为依据。未对用户文件实施破坏性复现，未开展密码学形式化验证、模糊测试或 XP 实机验收。本轮只新增本文档，没有实施下述程序修复，也没有重新构建发布文件。行号对应本次审查的源码快照。

优先级：

- **P0**：发布阻断，涉及已有数据误删、越界写入、静默完整性破坏或密钥丢失。
- **P1**：下一轮必须处理，涉及异常输入、失败一致性、还原语义和关键测试缺口。
- **P2**：体验、性能与进一步安全加固。

## 2. P0：必须先修复的四类问题

### R01. 加密失败清理会删除已有加密包

**证据：** `src/package.rs:450` 的 `create_package()` 在任意错误后，根据调用前的 `dst.exists()` 决定删除内容。目标原本存在时直接删除其 `blobs` 和 `package.chx`（第 457–465 行）。内层函数却可能只是因“目标非空”而拒绝执行（第 481–486 行），尚未创建任何属于本次任务的文件。CLI 在 `src/enc_app.rs:765` 直接调用此接口。

**影响：** 用已有包作为目标执行加密，本来应无副作用地报错，却可能删除旧包的密文和清单。源扫描失败也可能进入同一清理分支。错误提示不能弥补数据已被删除的后果。

**修复方案：**

- 禁止以“路径存在”推断“本次任务拥有这个目录”。
- 先完成输入校验，再以独占创建方式分配随机命名的暂存目录，记录本次任务的所有权。
- 清理仅限确实由本任务独占创建的暂存输出；前置校验失败不得执行删除。
- 完成后发布到最终目录，不静默替换已有目标；发布竞争失败时保留旧目标。
- 清理失败必须报告残留位置，不得无条件提示“已清理”。

**验收：** 在隔离测试目录中预置旧包与哨兵文件，分别触发目标非空、源不存在、源无文件、权限错误、取消；旧包和哨兵的内容哈希必须全部不变。

### R02. 清单路径缺少 Windows 安全校验，可逃出还原目录

**证据：** `Package::decode()` 接受未经验证的 `src_name`、文件路径和目录路径（`src/package.rs:309`、337、351）。`restore_target()` 直接 `out_root.join(src_name)`（第 971–977 行）。`util::join_rel()` 只按 `/` 拆分，跳过点段后直接 `PathBuf::push()`（`src/util.rs:169`）。它没有拒绝 Windows 的反斜杠上级路径、盘符、根路径、UNC 或 ADS。

**影响：** 恶意清单可以通过顶层名称改变整个还原根目录；文件或目录记录也可指向预期目录之外。例如反斜杠形式的上级路径不会被当前 `/` 拆分规则拦住。`restore_package()` 在块认证前执行 `File::create()`（`src/package.rs:797`），可截断当前用户有权限写入的既有文件。增加零长度记录还会在 `run_dec()` 第 842–844 行跳过密文校验。此问题不需要攻击者获知密钥，只需改动用户随后打开的包清单，并诱导用户使用原配密钥还原。

**修复方案：**

- `src_name` 必须是单个合法文件名组件；文件、目录记录采用严格定义的相对路径语法。
- 拒绝而不是悄悄清洗空组件、`.`、`..`、反斜杠、盘符、UNC/设备路径、NUL 和冒号；同时处理 Windows 保留设备名、尾随点及空格别名。
- 检测大小写不敏感目标上的同名冲突、文件/目录冲突和重复 blob 映射；UTF-8 损坏必须报错，不做有损替换。
- 路径校验必须位于共享包层，GUI 与 CLI 不能各自实现一套不一致的规则。
- 在私有暂存目录中还原，不跟随已有目录树中的 junction/reparse point。对可被其他进程替换的路径，补充基于句柄和文件身份的检查；单纯 `canonicalize` 后再打开仍有竞态窗口。
- 对“其他本机进程拥有相同权限并主动竞态替换文件”的防护边界明确说明，不能声称字符串前缀比较提供完整隔离。

**验收：** 覆盖上级路径、绝对路径、UNC、盘符相对路径、ADS、保留设备名、大小写重名及 junction 测试。越界路径全部在创建文件前拒绝，输出根之外的哨兵完全不变。

### R03. 明文清单没有整体认证，整包完整性不成立

**证据：** `check_key()` 只认证固定验证明文及包 ID（`src/package.rs:750`）。`chunk_aad()` 只绑定包 ID、相对路径、块号和块长（第 229–235 行）。文件总长度、文件数量、目录列表、修改时间、顶层名称等未得到整体认证。

**影响：**

- 删除文件记录、增加空文件、改变目录或时间，不必修改已有块的标签。
- 将恰好为 2 MiB 的文件声明为 1 MiB，首块的路径、块号和块长不变，程序只恢复首块，剩余密文被忽略。
- 将记录大小改为零时不再读取该文件的 blob。
- “密钥正确”只能证明校验块可解开，不能证明整个清单真实或全部文件完整。

这不是 XChaCha20-Poly1305 被破解，而是被认证的数据范围不足。

**修复方案：** 引入新格式版本，保持清单明文，但对完整、精确定义的清单字节做密钥认证。清单认证覆盖版本、包 ID、算法/块大小、顶层名、完整文件和目录集合、长度、时间、blob 编号及必要约束。

可使用经验证的域分离密钥派生后，对“空消息 + 清单作为 AAD”生成 AEAD 标签，或使用成熟 MAC 方案。认证标签本身不包含在被认证字节中；认证 nonce、用途域和编码边界必须明确。不要自行拼接一个未经验证的新密码协议。块 AAD 在新版本中加入用途域、格式版本、文件标识、完整文件大小和总块数，作为纵深防御。

无密钥时只进行有界解析和预览，并标注“清单尚未认证”；导入密钥后必须认证完整清单，认证失败不得创建输出。通过清单认证仍不意味着所有 blob 已校验，二者状态需要分开。

**验收：** 对清单每个字段做单独变异，包括整块截短、删除条目、增加空文件、改时间、改根目录名；必须在写出任何文件前失败。新格式的文件块替换、重排和跨包移植必须失败。

### R04. GUI 忽略密钥文件写入失败，可能错误提示成功

**证据：** `src/enc_app.rs:587` 工作线程在建包成功后用 `let _ = package::write_key_file(...)` 忽略错误，随后仍提交原来的成功结果。`finish()` 第 647–677 行据此显示“加密完成”和密钥路径。CLI 第 768 行会检查写入错误，但失败后仍可能留下只有密文、没有已持久化密钥的输出。

**影响：** 权限、磁盘满、路径冲突等可导致密钥未成功保存；用户若相信完成提示并退出，内存密钥丢失后包可能无法解密。源文件默认不删除，但不能依赖用户始终保留原件。

**修复方案：**

- 将“密文完成”和“包及密钥均安全保存”定义为不同状态；最终成功必须包含密钥写入、同步和回读验证成功。
- 失败时保留内存密钥，提供重试与另存路径，清楚说明未完成；不要重新生成密钥去覆盖当前任务状态。
- 使用随机且独占创建的临时密钥文件，禁止覆盖已有密钥。不要使用固定 `.tmp` 路径处理秘密。
- 同卷暂存可便于最终发布，但包目录与外部密钥路径通常无法作为一个跨路径、跨卷事务原子提交。必须设计恢复状态，不能承诺两个独立 rename 就是完整原子事务。
- 需要崩溃恢复时，可在受限暂存位置先持久化密钥并记录未完成状态，成功后再正式发布；需明确这种策略会暂时留下密钥，不能继续绝对承诺“取消从不落盘密钥”。

**验收：** 注入密钥输出拒绝访问、磁盘满、已有同名文件、rename 失败等错误；不得出现成功提示或误覆盖旧密钥。退出/崩溃恢复测试必须证明不会将不可解密的输出标记为完成。

## 3. P1：健壮性与还原正确性

### R05. 解密先截断目标，再验证内容，没有失败事务

**证据：** `src/package.rs:787` 起先创建所有目录和文件，再执行 `run_dec()`。错误或取消直接返回，没有暂存提交机制。GUI 在 `src/dec_app.rs:592` 仅询问是否覆盖，不能保证失败时保留旧文件。

**方案：** 默认还原到独占的新目录；先认证清单，再在暂存目录逐块验证和写入。全部成功、文件同步及元数据恢复完成后发布。覆盖已有目录应作为单独高级流程，包含备份和回滚，第一阶段宜直接禁止。失败只清理本任务创建的输出，且提示已写出的明文可能残留，普通删除不等于安全擦除。

**验收：** 在最后一个块篡改、解密中取消、磁盘满及进程中断时，旧目标文件哈希不变，最终目录不得包含冒充成功结果的半文件。

### R06. 清单解析可 panic，长度与资源预算不足

**证据：** `Package::decode()` 在两个变长字符串之后直接读取固定字段（`src/package.rs:309–317`），未在每次读取前检查剩余长度。`read_u32_le/read_u64_le` 使用直接索引。`open_package()` 第 742 行无上限读取整个清单。块大小、条目计数、总字节数及末尾多余字节缺少统一校验；`chunk_count()` 第 197 行和任务规划中存在未检查加法、窄化转换。

**方案：** 使用可返回 `Result` 的有界游标读取器；所有长度、偏移、块数、容量与加法乘法采用 checked 运算。为清单字节数、路径长度/深度、条目数、文件总量和任务队列内存规定可配置或明确的上限。现有格式只接受实际支持的块大小，不能让不可信 `chunk_size` 决定巨额分配。总大小必须由条目安全求和验证，拒绝重复、缺项及非预期尾随数据。密钥文件读取也应限制长度。

**验收：** 对有效清单的每个截断位置、畸形计数、极大块大小、整数边界和无效 UTF-8 做测试，GUI/CLI 都应返回可读错误，而非 panic、挂起、巨额预分配或内存耗尽。

### R07. 源文件扫描与读取不一致，可能静默漏文件或混合版本

**证据：** `src/package.rs:162–168` 忽略 metadata 错误并跳过符号链接，未返回跳过清单。扫描保存大小/时间后，`run_enc()` 第 647 行按任务重新打开文件。源文件期间增长可能只加密旧长度；等长修改可能得到多个时刻的数据混合。`src/util.rs:198` 的路径关系判断只是字符串变换，不能消除 `..`、链接和别名。

**方案：** 默认遇到无法枚举/读取项目时失败，或要求用户明确接受“不完整备份”并列出遗漏。统一源、输出、密钥路径冲突检查到共享层。用同一文件句柄读取并比较文件身份、长度和修改状态；可采用拒绝写共享的 Windows 打开策略，无法获得稳定读取时明确失败。仅前后比较时间不能证明原子快照；需真正快照保证时另行评估 VSS，并说明 XP 版本/服务的支持差异。

**验收：** 加密期间追加、截断、等长替换、重命名源文件，权限变化和链接输入均有确定结果，不得静默宣称完整成功。

### R08. “原样还原”目前不覆盖目录时间及全部 Windows 元数据

**证据：** `Scan.dirs` 和 `Package.dirs` 只记录名称。`src/package.rs:823` 将目录时间设为建包时间，而非原始时间。`src/util.rs:374` 的目录 `CreateFileW` 没有 `FILE_FLAG_BACKUP_SEMANTICS`，且打开/设置时间错误均不报告。第 158 行将 1970 年前时间归零；文件名使用 `to_string_lossy()`，也不能保证所有 Windows UTF-16 名称无损。ACL、创建时间、ADS、硬链接等未作为独立记录保存。

**方案：** 先明确支持契约：普通文件内容、文件/目录名称结构、文件/目录修改时间。为目录增加独立元数据；恢复目录时间要从最深层向根目录执行，顶层目录最后处理。设置失败返回“内容完成但元数据未完全恢复”的结果。新格式考虑原始 FILETIME 加显式有效标志，避免把合法时间零值当未知。特殊文件名、ADS、链接、ACL 等需明确拒绝/提示，或作为后续扩展，不再无条件声称 NTFS 对象完全复制。

**验收：** 文件、空目录、多层目录及根目录分别设置不同时间，覆盖 1970 年前、NTFS 精度、FAT 精度降级及属性写入失败；不能仅检查文件 mtime。

### R09. 密钥/清单写盘与任务错误传播不充分

**证据：** `src/util.rs:232` 的 `write_all_atomic()` 使用固定临时名，先删除旧目标再 rename，存在并发冲突与失败窗口。`src/package.rs:568` 直接写最终清单；第 706 行忽略 flush 错误。`src/pool.rs:178` 忽略工作线程 join 错误；`fail()` 不自动停止其它任务。

**方案：** 不覆盖模式使用独占创建；确需替换时设计符合 XP 的替换与备份流程。对包、密钥及状态同步错误逐层传递，不将 `flush()` 等同于掉电持久化。任务层返回明确错误并设置取消标志，避免已失败仍继续写大量数据。若 release 使用 panic=abort，应承认无法依靠 join 捕获恢复，必须避免不可信输入触发 panic，并依靠暂存协议保证崩溃后的安全状态。

**验收：** 并发启动、固定目标冲突、同步失败、任务异常和中途终止不得误报成功或删除他人输出。

## 4. UI、性能与验证改进

### U01. 列宽修复保留，但需要真实视觉回归

当前 `src/ui.rs:601` 已包含 `LVCF_WIDTH`，第 621 行也已正确把整数宽度传给 `LVM_SETCOLUMNWIDTH`。这两个原始缺陷不应再列为未修复漏洞。

仍可改善的地方：`layout_three_cols()` 第 629 行固定大小列 95、时间列 145，预留 24；窄宽度分支反而把文件列从约 120 提高到 220，存在宽度跳变。窗口重排还会重设用户手动拖动的列宽。固定 145 像素不能保证所有字体/DPI 的时间字符串都完整显示。

建议根据实际列表客户区和当前字体测量列首、最大常见大小及完整时间文本。文件名列占剩余空间且有合理最小宽度；极窄时明确采用横向滚动或限制窗口最小宽度。保留用户手动分配，提供明确的恢复默认方式。长路径允许省略号和提示查看全文，不能承诺任意长路径在固定列宽内完全显示。

验收覆盖两程序的空列表/有数据列表、长中文名称、深路径、垂直滚动条、默认/放大/最小窗口和 100%、125%、150%、200% DPI。读取列宽只是辅助证据，应同时保存并人工检查截图。

### U02. 大目录响应与进度语义

扫描和大量列表更新应移到可取消后台任务，主线程分批更新或采用虚拟列表，设置重绘抑制及适当批量通知。界面区分扫描、加密/解密、校验、发布阶段，不将数据处理达到 100% 等同于最终保存成功。

`src/pool.rs:23` 使用 32 位 `usize` 存 KiB，总量约 4 TiB 即有截断风险；`tick()` 逐任务向上取整会使大量小文件的累计进度偏大。建议在 XP/i686 可支持的同步机制下保存精确 u64 字节数，例如使用 Mutex 聚合计数，避免未经验证地引入目标平台不支持的原子实现。以大量 1 字节文件、超过 4 GiB 和 4 TiB 的模拟计数做单元测试。

`cpu_count()` 最多返回 16，但实际 `plan_threads()` 上限是 8（`src/pool.rs:119`）；UI 不应将逻辑 CPU 数当作真实任务线程数。线程数和队列深度按 HDD/SSD、小文件/大文件工作负载验证，不凭 SIMD 内核吞吐直接宣称整包速度。

### T01. 密码学实现测试不能只有自洽往返

`src/crypto.rs:832` 的 `xor_with()` 对所有非标量档位都调用自动分派。因此在 AVX2 机器上，名为 SSE2 的测试也可能实际测试 AVX2，不能证明各路径都通过。

建议保留已有效的已知答案测试，修正各档位强制分派，在检测到 CPU/OS 支持时分别执行，并把不支持标为 SKIP。增加独立实现（例如测试环境中的 libsodium）交叉验证的 XChaCha20-Poly1305 向量，覆盖空消息、空 AAD、各块边界、尾部和篡改。测试工具可以使用新工具链，不必进入 XP 发布二进制依赖。

对标量/向量代码、Poly1305 和恒定时间属性进行专项审查。当前没有证据证明算法核心已被攻破，也没有充分依据声称手写密码实现已经完成独立安全审计。优先评估经过审计且可满足旧工具链的成熟实现，不为“零依赖”牺牲可验证性。

### T02. XP 静态检查和现有 GUI 测试的结论需要收紧

- `tools/pe.py:20` 使用手工 Vista+ 黑名单，而非目标 XP 系统实际 DLL 导出集合；不足以证明所有静态导入可用。应根据明确的 XP SP/架构构造导入校验基线，并覆盖延迟导入、序号导入以及运行时 fallback。
- `tools/pe.py:42` 把 `GetFileInformationByHandleEx` 放入放行集合，应核对其最低系统要求；它属于 Vista 及以后 API。多项黑名单条目实际在 XP/更早系统已存在，也需逐项校正。不能靠一个不准确的表证明兼容。
- 发布物存在 `msvcrt.dll` 依赖时，应描述为依赖系统 CRT、无需另装特定运行库，而非未经限定的“完全静态 CRT”。严格静态 CRT 是独立需求，需按最终导入表和工具链验证。
- XP 兼容应增加隔离虚拟机/真实设备启动、加解密、随机数、线程、对话框和取消测试；PE 5.01 只是必要条件之一。旧 CPU 的最低 ISA 也需明确，不能把算法标量 fallback 解释为整个 Rust 二进制一定支持所有无 SSE2 的 XP 机器。
- `tools/drive-gui.ps1:101` 启动时已经传了密钥，第 104 行却把随后列表检查标为“without key”，不能独立证明无密钥预览。应拆成不传密钥的专用测试。
- `tools/inspect.ps1:13` 无条件传递默认空 `ArgumentList`，会导致此前出现的参数校验错误；仅在有参数时才传该参数，并将布局失败转换为非零退出码。
- `tools/drive-gui.ps1:148` 的 `Format-Table` 不适合作为机器可组合的结果流。应输出结构化结果，最后由展示层格式化；调用者检查真实退出码，不能在外层命令 exit 1 时宣称整个验收成功。
- `README.md:164` 仍引用已移除的 `tools/drive-dec.ps1`，需随测试入口一起修正文档。

### S01. 密钥保管与信息泄露提示

清单公开文件名是既定需求，但原始绝对源路径 `src_path` 通常不是还原所必需，会额外泄露用户名、盘符或网络共享位置，建议默认不保存。包与密钥放在同一父目录便于使用，却不提供对“整个目录被复制”的保密性，应支持单独选择密钥存放位置。

减少密钥克隆和内存驻留，使用适配旧工具链且不会被编译器优化掉的清零方法，退出/取消时处理已持有的密钥。需明确这不能防止同权限恶意进程、调试器、剪贴板历史、分页或崩溃转储获取秘密。可取消默认自动复制，敏感复制采用明确动作并提示风险。CRC32 仅检测意外损坏，不是防伪认证。

## 5. 建议的新格式和执行流程

### 新格式原则

1. 显式升级包版本，新写入默认使用带清单认证的版本；固定字节序、长度字段、编码和域分离规则。
2. 明文清单可预览，但标记未认证；任何资源分配和 UI 展示前先完成有界解析。
3. 完整认证覆盖目录及文件元数据；文件块额外绑定文件整体长度/块数和唯一标识。
4. 空文件与空目录也属于认证集合；blob 头、声明长度、实际长度和数量严格一致。空文件的表示方式需明确定义，不能依靠“没有块所以无需检查”。
5. 目录元数据可独立保存；限制、非法输入和扩展字段处理规则写入格式规范。

### 建议工作流

加密：输入校验与稳定扫描 → 独占暂存 → 安全保存任务密钥/恢复状态 → 加密及完整性检查 → 生成认证清单 → 同步并发布包和密钥 → 回读核验 → 最终成功。

解密：有界解析并预览 → 导入密钥 → 认证清单 → 路径/资源/目标检查 → 独占暂存 → 验证密文并还原 → 检查内容与元数据结果 → 发布 → 最终成功。

对已有旧格式包：不能假装缺失的历史认证能够补回。可提供显式“旧格式恢复模式”，强制安全路径、严格限额和全新输出目录，并警告无法证明原始清单未被删改。只有与可信原始文件或可信独立索引核对后再迁移，才能更强地确认历史完整性。给当前旧清单补标签只保护补签后的状态，不证明过去未被篡改。

## 6. 实施顺序与发布门槛

| 阶段 | 工作 | 通过条件 |
| --- | --- | --- |
| A：止损 | R01 清理所有权、R02 路径、R04 密钥错误、R05 暂存还原 | 失败不损坏已有数据，不越界写，不把缺密钥输出报成功 |
| B：格式安全 | R03 清单认证、R06 有界解析、新格式及旧包策略 | 所有清单变异在写盘前拒绝，解析无 panic/失控分配 |
| C：真实性 | R07 源一致性、R08 元数据、R09 同步与错误传播 | 无静默漏项，异常有明确结果，崩溃状态可辨识 |
| D：验收与体验 | T01/T02、列宽/DPI、后台扫描和真实进度 | 独立密码向量、XP 实测、真实列宽截图和测试退出码均可复核 |

每阶段增加针对性测试，而不是只重复原来的 30 项。发布前至少满足：

- 旧包/旧密钥/外部哨兵在所有拒绝和失败路径下保持不变。
- 错密钥、坏标签、缺块、截短、改清单、增删空文件均有负面测试。
- 文件内容哈希、名称、目录集合和声明支持的元数据分别验收。
- 密钥保存失败、磁盘满、权限不足、取消和进程中断不产生假成功。
- GUI 与 CLI 使用相同校验逻辑，结果与退出码一致。
- 两端在不同 DPI 和窗口尺寸下保存有数据的截图；列首与日期可读。
- 明确列出已验证的 Windows/SP/CPU/文件系统矩阵；未实测的不写成已保证。

## 7. 当前使用建议

修复完成前保留原始文件，不将当前加密包作为唯一备份；不要覆盖已有包或已有还原目录；不要解密来源不可信或可能被改动的包。保存密钥后单独备份，并在新的空目录试解，比较完整文件/目录集合与内容哈希。该操作建议降低误用风险，但不替代上述代码和格式修复。
